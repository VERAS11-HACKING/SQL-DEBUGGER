# SQL-DEBUGGER
SQL Debugger is a Swift-based Xcode application that helps users write, test, and debug SQL queries. It detects syntax errors, explains issues, suggests possible solutions, and displays query results through a simple, fast, and user-friendly interface.
